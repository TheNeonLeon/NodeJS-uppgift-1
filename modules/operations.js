/*const db = require('../server');
const routes = require('../modules/routes');
const uuid4 = require('uuid4');
const bodyParser = require('body-parser');



const addToCart = () => {
   const checkProd = db.get('varukorg').find({id}).value();
        if(!checkProd){
            let message = ""
            return message;
        }
}
*/
